group 'com.jiguang.jpush'
version '1.0-SNAPSHOT'

buildscript {
    repositories {
        mavenCentral()
        google()
//        jcenter()
        // fcm
        maven { url "https://maven.google.com" }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2'
        // fcm
        classpath 'com.google.gms:google-services:4.4.0'
    }
}

rootProject.allprojects {
    repositories {
        mavenCentral()
        google()
//        jcenter()
        //fcm
        maven { url "https://maven.google.com" }
    }
}

apply plugin: 'com.android.library'

android {
    compileSdk 33

    defaultConfig {
        minSdk 23
        testInstrumentationRunner 'androidx.test.runner.AndroidJUnitRunner'
        consumerProguardFiles 'consumer-rules.pro'
    }
    lintOptions {
        disable 'InvalidPackage'
    }
}

dependencies {
    implementation 'cn.jiguang.sdk:jpush-google:5.2.0'
//    implementation 'cn.jiguang.sdk:jcore-google:4.2.0' # since 5.0.0 auto implementation
    // fcm same version : jpush-google
    implementation 'cn.jiguang.sdk.plugin:fcm:5.2.0'
    implementation 'com.google.firebase:firebase-messaging:23.2.1'
}

// fcm  project/app/build.gradle
//apply plugin: 'com.google.gms.google-services'
